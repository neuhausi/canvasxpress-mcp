# Core — always required
fastmcp>=2.0.0
anthropic>=0.40.0
sqlite-vec>=0.1.0
sentence-transformers>=3.0.0
numpy>=1.24.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Amazon Bedrock (LLM_PROVIDER=bedrock)
# pip install boto3
# boto3>=1.34.0

# OpenAI / corporate gateway (LLM_PROVIDER=openai or openai_corporate)
# pip install openai
# openai>=1.30.0

# Google Gemini (LLM_PROVIDER=gemini)
# pip install google-generativeai
# google-generativeai>=0.8.0
